# Veyra — Roadmap

**Живой план проекта. Отсюда ведётся работа.** Отмечай статус задач,
двигай по этапам. Это рабочий документ — в отличие от `STATUS.md` (снимок
на дату) он не устаревает, а обновляется по ходу.

Где что: требования — `TZ-Veyra.md` · кейсы — `USE-CASES.md` · расхождения
с экосистемой Avito — `GAP-ANALYSIS-AVITO.md` · загрузка корпуса —
`SCALE-PLAN.md`.

Обозначения: `[x]` сделано · `[ ]` в очереди · `[~]` в работе · `⏸` отложено.

---

## Где мы сейчас

Прототип. Живой путь (Claude → `veyra-mcp` → корпус) собран. Детекторы
логики и обещаний валидированы (83% / 94%); числовые расхождения отданы
Claude на запросе. Корпус — 3 документа (пробный стенд). **Главный блокер
движения дальше — реальный корпус.**

---

## ✅ Сделано

- [x] Детекторы провалидированы eval-харнесом — логика 83%, обещания 94%
- [x] Числовой детектор: предвычисление закрыто, числа ищет Claude на запросе
- [x] Регрессионный харнес (`scripts/regression.py`) + git-хук pre-push
- [x] Светофор критичности (severity) + 2-табличный вывод в `SKILL.md`
- [x] Изоляция legacy `/api/chat` (флаг `ENABLE_LEGACY_CHAT`)
- [x] Gap-анализ против Avito, `USE-CASES.md`, обзорный и статус-слайды
- [x] Убран 48K-потолок чанкинга

---

## ▶ Сейчас — интеграция в экосистему Avito  *(фокус недели)*

- [ ] **`veyra-mcp` → PR в `mcp-registry`** — регистрация за MCP Hub.
      Закрывает OAuth (Keycloak) и PII-анонимизацию разом. **Срок: эта неделя.**
      План и чек-лист — `MCP-HUB-ONBOARDING.md`.
- [ ] **Подключиться к инициативе общих Golden Sets** (Д. Колесников) —
      написать, договориться использовать общий набор, свой не строить.
- [ ] **Показать Veyra Алексею** — слайды готовы (`veyra-overview.html`,
      `veyra-status.html`). Запросить экспорт корпуса из Confluence.
- [ ] Синхрон с В. Сыровым — Veyra как движок его Portfolio Assistant,
      не дублировать.

---

## ⏭ Дальше — корпус и масштаб  *(после получения корпуса)*

- [ ] Загрузить корпус батчами 10 → 30 → 100 → 300, гейт после каждого
      (`SCALE-PLAN.md`)
- [ ] Метаданные из Confluence (статус / дата / иерархия / сегмент)
- [ ] Retrieval на масштабе: корпусный FTS5-индекс, hierarchy boost,
      `find_gaps` — чинить уже с измеримым результатом
- [ ] Завести baseline регрессии на реальном корпусе

---

## ⏳ Потом — валидация и релиз

- [ ] ⏸ Независимая разметка ground truth аналитиком — *отложено по
      решению; вернуться, когда будет корпус и ресурс аналитика*
- [ ] Публикация `veyra-skill` в Skills Hub
- [ ] Пилот на 3 пользователях ASD Goods

---

## Бэклог / попутно

- [ ] G6 — логирование стоимости и числа tool-call в `veyra-mcp`
- [ ] Проактивный ночной дайджест расхождений (Фаза 4 ТЗ)

---

## Решения проекта

| Решение | Статус |
|---|---|
| Числовой детектор как предвычислитель — закрыт; числа ищет Claude | принято |
| Разметка ground truth аналитиком — отложена | принято |
| `veyra-mcp` за MCP Hub | в работе, эта неделя |
| Общие Golden Sets — подключаемся, свой не строим | принято |

---

## Риски / блокеры

- **Корпус** — без экспорта Confluence всё после блока «Сейчас» стоит.
  Главный блокер.
- **LLM-доступ** — личный ключ упирается в rate-limit; целевое — Avito
  LLM Gateway.
- **Дублирование** — Portfolio Assistant В. Сырова решает близкую задачу;
  не синхронизироваться = риск построить параллельное.
