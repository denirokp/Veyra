# ТЗ — Veyra

**Версия:** 1.4
**Дата:** 17 мая 2026
**Заказчик:** Денис Копылов — P&S marketing / ASD Goods, Avito
**Куратор:** Алексей Ли

**История версий:**
- v1.1 — базовый сценарий + параллельные треки гейта и skeleton MCP.
- v1.2 — полный набор возможностей задачи Алексея, классификация «детектор / генератор / обогатитель».
- v1.3 — сверка с дословным брифом Алексея от 07.05.2026: добавлена интеграция с дашбордами (Возможность 7), серые зоны отделены от слепых метрик, помощь в написании понижена в приоритете как не входящая в бриф.
- v1.4 — поправки по итогам стресс-теста логики (см. раздел 13): введена Фаза 1.5 (retrieval-гейт), уточнена формулировка «память + ретрив + LLM» вместо «слой детекторов», ужесточён контракт ответа и ground truth. Разделы 4 и 7 синхронизированы с фактической кодовой базой (retrieval — гибрид эмбеддинги + BM25 + RRF, не TF-IDF; актуальный состав скиллов — в `SYSTEM.md §5`).

> **Перед чтением:** раздел 13 содержит существенные поправки v1.4. При
> конфликте раздел 13 имеет приоритет над разделами 1–12.
>
> **Имя файла:** документ называется `TZ-Veyra.md` без версии в имени —
> актуальная версия указана в шапке. Так имя файла не устаревает с
> выпуском новых версий.

---

## 1. Контекст

Veyra — инструмент для поиска расхождений, противоречий, невыполненных обещаний и слепых зон в стратегических материалах коммерческого блока Avito. Аудитория — аналитики и менеджеры ASD Goods, потенциально весь коммерческий блок.

Источники данных — **два типа**:
- **Документы:** 500+ стратегических документов (Confluence, частично docx/pdf).
- **Дашборды и метрики:** редаши и DWH-данные по стримам команды (см. Возможность 7).

Данные конфиденциальные — это отдельный фактор архитектуры (см. Принцип 5).

**Целевой пользовательский сценарий:**

```
Пользователь: "Проверь инициативу X на согласованность с корпусом"
Veyra:        список расхождений с цитатами и ссылками на источники
```

**Метафора Алексея — «бульон»:** загрузить по максимуму материалов и собрать переиспользуемый субстрат знаний, из которого дальше «варится» любой ответ — разбор инициативы, поиск противоречий, проверка обещаний.

Дополнительно (опционально, Фаза 4): фоновый проход по корпусу с проактивными уведомлениями о новых расхождениях.

---

## 2. Принципы реализации (не нарушать)

1. **Никакой галлюцинирующий детектор не попадает в прод.** Skeleton MCP можно строить параллельно с гейтом, но реальная логика детектора заливается в MCP-сервер только после прохождения гейта. До гейта эндпоинты возвращают заглушки. Сервис не регистрируется в production-режиме MCP Hub — только staging/alpha.

2. **Использовать готовую инфру Avito, не строить свою.**
   - LLM: внутренний gateway `https://llm-gateway.k.avito.ru/v1` (OpenAI-совместимый), модель `vsellm/anthropic/claude-opus-4.7`.
   - Документы: Confluence MCP из mcp-hub.
   - Дашборды/метрики: `dwh-redash-mcp`, `querypad`, `trisigma-mcp` из mcp-hub.
   - Хостинг сервиса: PaaS Avito.
   - Регистрация: avito mcp-registry + skills-hub.

3. **Veyra = слой детекторов, а не «ещё один продукт».** Не строить отдельный сайт/UI. Точка входа — skill в Avito AI, плюс опционально n8n workflow для проактивности.

4. **Образец для упаковки skill** — `quality-metrics` (`stash.msk.avito.ru/agent/qa-skills`). Структура `SKILL.md` + `scripts/` + `references/`, два режима (диалог + Confluence-отчёт).

5. **Конфиденциальность.** Индекс с эмбеддингами и любые выгрузки метрик — производное от конфиденциальных данных. Хранение только в авторизованном сервисе (MCP-сервер с Keycloak), не в общем S3, не локально у пользователей.

6. **Детектор и генератор валидируются по-разному.** Детектор (находит факт о корпусе — расхождение, обещание, пробел) проходит гейт recall/precision. Генератор/обогатитель (пишет текст, тянет внешние данные) проходит свой бар качества. Не смешивать в одну метрику. См. раздел 3.

---

## 3. Возможности (полный набор — задача Алексея, бриф 07.05.2026)

Возможности **не однородны по риску** и валидируются по-разному.

| # | Возможность | Класс | Данные | Риск | Как валидируется | Фаза |
|---|---|---|---|---|---|---|
| 1 | Числовые расхождения | детектор | корпус | низкий — детерминированная группировка метрик | гейт, трек 5.1 | 1 |
| 2 | Логические расхождения | детектор | корпус | **высокий** — галлюцинация связей | гейт, трек 5.2 (отдельно) | 1 |
| 3 | Невыполненные обещания | детектор | корпус + даты | средний | гейт, трек 5.3 | 1–2 |
| 4a | Пробелы — «провалили между стульев» | детектор | корпус | **очень высокий** — доказательство отрицания | гейт, трек 5.4 | 2 |
| 4b | Неувиденные возможности и гипотезы | детектор поверх внешних рамок | корпус ↔ рынок ↔ дашборды | **очень высокий** — доказательство отрицания | через Возм. 5 и 7B + свой бар (см. ниже) | 2–3 |
| 5 | Рыночный контекст (кросс-опыление) | обогатитель / рамка для 4b | внешний веб | средний — выдуманные источники, утечка | свой бар: каждое утверждение = рабочая ссылка | 2–3 |
| 6 | Помощь в написании | генератор | корпус + стиль | средний | не contradiction-гейт; черновик прогоняется через детекторы 1–4 | 3+ (опц.) |
| 7 | Дашборды и слепые метрики | интеграция + детектор / рамка для 4b | дашборды/DWH | A: низкий · B: высокий | A: ручной бенчмарк · B: гейт, трек 5.5 | 2–3 |

**Ключевой вывод:** возможности 1, 2, 3, 4a, 7B — детекторы, каждая идёт через свой трек гейта раздельно. Возможность 4b — составная: отдельного трека гейта не имеет, валидируется барами Возможностей 5 и 7B. Возможности 5, 6, 7A — не детекторы, у них свои бары качества.

> **Два вопроса под одним словом «серые зоны».** В брифе Алексея это
> **два разных пункта**, и их нельзя мерять одним детектором:
> - **4a «Пробелы»** — что обсуждали/обещали и **бросили**. Смотрит
>   назад. Выводится **из корпуса** (что упомянули, но не закрыли планом).
> - **4b «Неувиденное»** — какие новые идеи/гипотезы/направления мы
>   **упускаем**. Смотрит вперёд. **Из корпуса не выводится** — нельзя
>   найти то, чего в документах нет, читая то, что в них есть.
>
> **4b отвечается через три внешние рамки отсчёта** (Алексей: «запрос по
> возможностям, которые мы не видим»):
>
> | Рамка | Что сравниваем | Реализуется через |
> |---|---|---|
> | Корпус ↔ рынок | идеи/практики снаружи, которых нет у нас | Возможность 5 |
> | Корпус ↔ дашборды | метрики, которые стоит смотреть, но не смотрим | Возможность 7B |
> | Корпус ↔ другие направления Avito | что делают соседние стримы и применимо нам | Фаза 3 (внутр. кросс-опыление) |
>
> Поэтому Возможности 5 и 7 — **не «вспомогательные»**: это механизм
> ядровой задачи 4b. Без них «что мы не видим» не отвечается в принципе.

### 3.1. Возможность 1 — Числовые расхождения
Одна метрика, один период, одна единица — разные значения в разных документах. Детектор по корпусу, детерминированная группировка. Риск низкий.

### 3.2. Возможность 2 — Логические расхождения
Противоположные подходы/тезисы по одной теме («здесь вы говорите одно, здесь другое»). Детектор по корпусу, LLM-анализ. **Риск высокий** — Claude на длинных стратегиях склонен выдумывать логические связи.

### 3.3. Возможность 3 — Невыполненные обещания
Обещание в документе A, чей статус «не выполнено» виден в документе B или по срокам. Детектор по корпусу + временная привязка.

### 3.4. Возможность 4 — Что мы не видим (4a пробелы + 4b неувиденное)

Под «серыми зонами» в брифе скрыты **две разные задачи** — см. блок
выше. Здесь они описаны раздельно.

**4a. Пробелы — «провалили между стульев».** Темы и риски, которые
упомянуты/подразумеваются в корпусе, но не закрыты планом — обсуждали и
забыли. Детектор **по корпусу** (set-difference: темы корпуса минус темы
стратегии). **Риск очень высокий** — «чего-то нет» требует доказать
отрицание. Жёсткое правило: каждый пробел сопровождается **охватом
поиска** (что и где проверено), иначе находка отбрасывается. Реализован
скиллом `find_gaps`.

**4b. Неувиденные возможности и гипотезы.** Какие новые идеи, гипотезы,
направления мы упускаем — то, чего в корпусе нет вообще. Из корпуса
напрямую **не выводится**: нужна внешняя рамка отсчёта. Реализуется
**не отдельным скиллом, а логикой поверх Возможностей 5 и 7B** (см.
таблицу трёх рамок выше). Это новое: текущий `find_gaps` задачу 4b
**не решает** — он покрывает только 4a. **Риск очень высокий.** Бар
качества — комбинированный: рыночные утверждения с рабочими ссылками
(как у Возм. 5), метрики с привязкой к стриму и дашборду (как у 7B).

### 3.5. Возможность 5 — Рыночный контекст (кросс-опыление)
Кросс-опыление с рынком: внешние данные сопоставляются с внутренними
документами. Две роли: (1) при разборе инициативы — рыночный контекст;
(2) **рамка отсчёта для Возможности 4b** — что есть на рынке, чего нет у
нас. **Не детектор расхождений** — гейт расхождений неприменим. Свой
бар: каждое рыночное утверждение несёт рабочую ссылку на источник; на
проверке вручную сверяются N утверждений. Это **не «вспомогательная»**
возможность — без неё 4b не отвечается.
**Конфиденциальность критична:** запрещено отправлять сырой текст конфиденциальных документов во внешний веб-поиск. Запросы строятся из обобщённых тематических терминов. Эндпоинт принимает обобщённую тему, не документ.

### 3.6. Возможность 6 — Помощь в написании
Помощь в написании инициативы/раздела в стиле команды. **Генератор.** Veyra не пишет документ автономно: (а) собирает grounded-факты из корпуса под тему, (б) даёт черновик в стиле команды, (в) сразу прогоняет черновик через `check_initiative`. Реализуется как расширение режима отчёта скилла.
> **Примечание:** этой возможности нет в брифе Алексея 07.05 — добавлена из кодовой базы и прошлых обсуждений. Первый кандидат на вырез из MVP при дефиците времени.

### 3.7. Возможность 7 — Дашборды и слепые метрики
Бриф Алексея дословно: «…чтобы код имел доступ к нашим всем дашбордам, чтобы была история метрик, динамики по стримам. И чтобы мы задали вопрос: какие метрики, на твой взгляд, могли бы быть ещё, на которые мы не обращаем внимания».

- **Часть A — ретрив живых метрик:** история метрик и динамика по стримам. Класс — интеграция; гейт не нужен, нужен ручной бенчмарк релевантности. Veyra-MCP вызывает `dwh-redash-mcp` / `querypad` / `trisigma-mcp`.
- **Часть B — слепые метрики:** «какие метрики стоит смотреть, но не смотрим». Это **детектор** (доказательство отрицания) → обязательно гейт, трек 5.5. Предложенная метрика привязывается к реальному стриму и реальному дашборду, иначе отброс. 7B — **вторая рамка отсчёта для Возможности 4b**: «неувиденное» в плоскости метрик.

Данные дашбордов конфиденциальны — то же хранение, что у индекса корпуса (Принцип 5).

> **Инфраструктура.** В Avito AI уже есть преднастроенные коннекторы для
> доступа к Confluence и редашам (отмечено на встрече 17.05). Это
> частично снимает инфра-риск Возможности 7 и 4b — отдельную интеграцию
> с нуля строить не нужно, задача — подключить её к контексту Veyra.

---

## 4. Что уже существует (НЕ ПЕРЕДЕЛЫВАТЬ)

Перед началом работы Claude Code должен:
1. Сделать `git status` на ветке `I209k`.
2. Просмотреть существующие файлы, особенно:
   - `index_docs.py` — индексатор корпуса;
   - `run_gate.py` — скрипт гейта детекторов;
   - `.env` / `.env.example` — конфигурация;
   - существующие скиллы в `backend/app/skills/` — актуальный полный
     состав (~17 модулей: детекторы `find_contradictions`,
     `find_logic_signals`, `find_intra_contradictions`, `track_promises`,
     `find_gaps`, `suggest_missing_metrics`; обогатители и навыки
     `market_agent`, `web_research`, `deep_research`, `document_brief`,
     `grounding`, `query_planner`, `self_check`, `plan_announcer`,
     `build_graph`, `extract_entities`, `initiative_review`) описан в
     `SYSTEM.md §5`; здесь список не дублируется, чтобы не устаревал.
3. Прочитать README, если есть.
4. Понять текущее состояние кода ПРЕЖДЕ чем что-то менять.

**Принцип:** адаптировать существующий код, не переписывать с нуля. Если что-то непонятно — спросить пользователя, не угадывать.

> **Известное состояние (на 17.05):** логический детектор (`find_logic_signals`) и детектор пробелов слабые — закладывать, что после первого гейта их придётся переписывать. Это план, не риск.

---

## 5. Гейт детекторов — пять треков

Гейт проверяет **пять треков раздельно**. Метрики не усредняются: числовой трек может пройти, а логический провалиться — это разные решения.

**Входы:** 3 заранее выбранных документа из корпуса + размеченный вручную ground truth по каждому треку.

| Трек | Возможность | Ground truth (минимум) |
|---|---|---|
| 5.1 Числовой | 1 | 2 числовых расхождения |
| 5.2 Логический | 2 | 2 смысловых противоречия |
| 5.3 Обещания | 3 | 1 обещание со статусом «не выполнено» |
| 5.4 Пробелы | 4a | 1 пробел «между стульев» (с охватом поиска) |
| 5.5 Слепые метрики | 7B | 1 метрика, которую не отслеживают, но должны (привязка к стриму+дашборду) |

**Что сделать:**
1. Проверить, что `index_docs.py` отрабатывает на 3 доках без ошибок.
2. Запустить `run_gate.py` на выборке.
3. Сравнить находки детектора с ground truth по каждому треку.
4. Записать метрики в `gate_report.md` — recall и precision **по каждому треку отдельно**. Каждая находка проверяема по точным цитатам из источника.

**Критерии прохождения (по каждому треку отдельно):**
- Recall ≥ 80%.
- Precision ≥ 70%.
- Все находки имеют точные цитаты из источника.

**Если трек не пройден:**
1. Разобрать false positives (что принято за находку, но не является).
2. Разобрать false negatives (что пропущено).
3. Доработать промпты детектора.
4. Повторить.
5. Если после 3 итераций трек не проходит — эскалация пользователю, возможен пересмотр самого детектора.
6. Skeleton MCP (Трек B) продолжает развиваться, но реальная логика непройденного детектора в него не заливается.

**Аутентификация на этой фазе:** временно личный ключ Anthropic пользователя через `.env`.

---

## 6. Фазы

### ФАЗА 1 — Параллельный запуск (1–3 дня)

Два трека одновременно.

**Трек A — Гейт детекторов (начать первым).** См. раздел 5. Цель — доказать, что детекторы находят настоящие расхождения и не галлюцинируют. Срок: полдня–день на трек.

**Трек B — Skeleton MCP-сервер (параллельно).** Цель — убрать инфраструктурный риск заранее (аппрув PaaS, Keycloak, требования mcp-registry).
1. Создать PaaS-сервис `veyra-mcp` (Python, FastAPI) — пустой шаблон.
2. Эндпоинты-заглушки: `check_initiative`, `find_contradictions`, `search_corpus`, `get_document` → `{"status": "not_implemented", "stub": true}`; `health()` → реальный 200 OK.
3. Подключить OAuth/Keycloak — без выдачи доступа кому-либо, кроме разработчиков.
4. Задеплоить на staging PaaS.
5. Draft PR в `mcp-registry` с пометкой `[ALPHA / WIP — endpoints return stubs]`. Не мерджить до прохождения гейта.

Чего НЕ делать на Фазе 1: не вызывать LLM из эндпоинтов; не подключать индекс; не мерджить в production mcp-registry; не звать пользователей.
Срок: 2–3 дня (зависит от аппрува PaaS). Owner: Денис + Злата, SLA — best-effort.

**DECISION POINT — гейт пройден?**
- Нет → возврат в Трек A. Skeleton остаётся skeleton.
- Да → Фаза 2.

### ФАЗА 2 — Наполнение (неделя 1–2, параллельные треки)

**Трек C — Переход на Avito LLM Gateway.** Предусловие: получен Bearer-токен (Анастасия Рысмятова / Олег Королёв).
1. Обновить `.env`: `LLM_BASE_URL=https://llm-gateway.k.avito.ru/v1`, `LLM_API_KEY=<sk-avi-…>`, `LLM_MODEL=vsellm/anthropic/claude-opus-4.7`.
2. Проверить: `curl -s -H "Authorization: Bearer $LLM_API_KEY" "$LLM_BASE_URL/models" | grep claude-opus-4.7`.
3. Если используется официальный SDK Anthropic — заменить на OpenAI-совместимый клиент (`openai` Python SDK с `base_url`).
4. Перезапустить гейт на gateway-ключе.
5. Критерий: результат гейта не отличается от Фазы 1 более чем на 5 п.п.

**Трек D — Индексация полного корпуса.**
1. Получить полный корпус (500+ документов).
2. Запустить `index_docs.py` на всём корпусе.
3. Сохранить артефакт индекса: ChromaDB persistent dir (векторные коллекции кусков) + SQLite (документы, чанки, карточки, метаданные id/title/url/last_modified).
4. Загрузить артефакт в storage движка на PaaS (volume / managed storage).
5. Бенчмарк: 10 тестовых запросов «найди документы про X» → проверка релевантности руками.
6. Критерий: топ-5 содержит хотя бы один релевантный документ в 9 случаях из 10.

**Трек E — Наполнение MCP реальной логикой.** Предусловие: гейт пройден.
1. Заменить заглушки реальными вызовами:
   - `check_initiative(text)`: retrieval → топ-N доков → детектор → структурированный ответ;
   - `find_contradictions(doc_id)`: то же, вход — документ из корпуса;
   - `find_open_promises(scope)`: невыполненные обещания (после трека 5.3);
   - `find_gaps(scope)`: пробелы «между стульев» (Возм. 4a) с охватом поиска (после трека 5.4);
   - `search_corpus(query, top_k)`: чистый retrieval;
   - `get_document(doc_id)`: полный текст для просмотра цитат.
2. Подключить хранилище индекса.
3. End-to-end тест на staging: вызов через mcp-hub → ответ <30 сек на типичный документ.
4. Обновить PR в mcp-registry: убрать `[ALPHA]`, добавить описание и owners.
5. Аппрув mcp-registry → мердж → сервис в production-hub.

**Трек F — Драфт SKILL.md.** Может идти параллельно. Образец — `quality-metrics`, изучить его `SKILL.md` первым делом.
Структура: `veyra-skill/SKILL.md` + `scripts/check_init.py` + `scripts/report.py` + `references/README.md`.
`SKILL.md` description (триггеры):
```yaml
---
name: veyra
description: >
  Находит расхождения, противоречия и невыполненные обещания в стратегических
  документах коммерческого блока. Используй при запросах "проверь инициативу",
  "согласована ли стратегия", "нет ли противоречий с прошлыми решениями",
  "что мы обещали и не сделали". Работает с корпусом 500+ документов через
  MCP-сервер veyra-mcp.
---
```
Два режима: диалог (`check_initiative` → структурированный ответ с цитатами) и отчёт (Confluence-страница, 3-колоночная таблица: расхождение / источник / рекомендация).

**Трек G — Интеграция с дашбордами (Возможность 7A).**
1. Подключить `dwh-redash-mcp` / `querypad` / `trisigma-mcp` как источники метрик.
2. Эндпоинт `metrics_history(stream)` — история и динамика метрик по стриму.
3. Ручной бенчмарк релевантности.
Детектор слепых метрик (7B) реализуется в Треке E после прохождения трека гейта 5.5 и зависит от Трека G.

### ФАЗА 3 — Запуск (неделя 2–3)

Предусловие: треки Фазы 2 закрыты.
1. End-to-end тест: установить skill → «проверь инициативу X» → осмысленный ответ.
2. PR в репозиторий skill (`agent/skills` или `agent/marketing-skills` — уточнить).
3. После мерджа skill виден через `avito ai skills search "расхождения"`.
4. Пилот: 3 человека из ASD Goods ставят и тестируют на реальных инициативах.
5. Собрать фидбек — что работает, какие false positives остались.

**Трек H — Рыночный контекст (Возможность 5).** Эндпоинт `market_context(topic)` на обобщённую тему. Веб-источник — уточнить (открытый вопрос 10). Каждое утверждение с рабочей ссылкой.

**Возможность 4b — Неувиденное.** Отдельного трека не имеет: собирается
как логика поверх Треков G (7B — слепые метрики) и H (5 — рынок), когда
обе рамки готовы. Запрос пользователя «какие возможности/идеи мы не
видим» → Claude сверяет корпус с рыночным контекстом и с метриками
дашбордов и формулирует гипотезы — каждая с указанием, на какой внешней
рамке основана.

**Помощь в написании (Возможность 6)** — опционально на этой фазе или после пилота. Первый кандидат на вырез при дефиците времени.

**Критерии готовности фазы:** skill ставится через `avito ai skills install veyra`; ≥3 пользователя ASD Goods попробовали и дали фидбек; recall/precision на реальных запросах не хуже гейтовых.

### ФАЗА 4 (опционально) — Проактивность через n8n

Предусловие: Фазы 1–3 пройдены, есть подтверждённый интерес пользователей.
1. n8n workflow по cron (раз в сутки): `veyra-mcp.scan_corpus_for_new_contradictions(since=yesterday)` → дайджест → пост в Mattermost-канал команды.
2. Шаблон — кейс Артёма Ивангородского (Risk Digest, AI Demo 29.04.2026).

---

## 7. Технический стек

- **Язык:** Python 3.11+.
- **LLM:** Avito gateway (OpenAI-совместимый клиент, `openai` Python SDK).
- **Retrieval:** гибридный — векторные эмбеддинги (`sentence-transformers`, локальная модель) в ChromaDB + BM25 + RRF-слияние + boost по иерархии. TF-IDF в реализации не используется.
- **MCP-сервер:** FastAPI на PaaS Avito.
- **Хранилище индекса:** ChromaDB (persistent dir) + SQLite — на диске сервиса / managed storage.
- **Дашборды/метрики:** `dwh-redash-mcp`, `querypad`, `trisigma-mcp` из mcp-hub.
- **Skill:** `SKILL.md` + Python scripts.
- **n8n:** для опциональной проактивности (Фаза 4).

---

## 8. Чего НЕ делать

- ❌ Не заливать реальную логику детектора в MCP-сервер до прохождения гейта (skeleton — можно).
- ❌ Не мерджить PR в mcp-registry до прохождения гейта (только draft с пометкой alpha).
- ❌ Не использовать API Anthropic напрямую в проде (только через gateway).
- ❌ Не публиковать индекс в общий S3 и не класть локально пользователям (конфиденциальные данные).
- ❌ Не строить отдельный сайт/UI — Veyra это скилл, не продукт.
- ❌ Не дублировать функциональность Confluence-поиска — у Veyra своя ниша (логика противоречий, не релевантность).
- ❌ Не звать пользователей тестировать, пока skill не прошёл end-to-end тест разработчиком.
- ❌ Не отправлять сырой конфиденциальный текст во внешний веб-поиск — только обобщённые темы.
- ❌ Не выдавать рыночное утверждение без рабочей ссылки на источник.
- ❌ Не писать документы за пользователя автономно — Veyra даёт grounded-черновик + self-check, финальный текст пишет человек.
- ❌ Не выдавать «серую зону» или «слепую метрику» без указания охвата поиска (что и где искали).
- ❌ Не усреднять метрики гейта по детекторам — пять треков отчитываются раздельно.

---

## 9. Открытые вопросы (уточнить у пользователя перед стартом)

1. Какие 3 документа использовать для гейта? (пути или URLs)
2. Ground truth трека 5.1 — какие числовые расхождения? (список с цитатами)
3. Ground truth трека 5.2 — какие логические противоречия?
4. Ground truth трека 5.3 — какое невыполненное обещание?
5. Ground truth трека 5.4 — какой пробел «между стульев» (Возм. 4a)?
6. Ground truth трека 5.5 — какая слепая метрика? (привязка к стриму и дашборду)
7. Где лежит полный корпус 500+ документов? (директория / Confluence-space / экспорт)
8. Получен ли токен на gateway, или работаем на личном ключе пока?
9. Owner будущего MCP-сервера — Денис, Денис+Злата, или просим у AI-платформы?
10. Чем делать рыночный трек — есть ли веб-поиск MCP в mcp-hub, или Tavily/Brave через прокси? Как согласовать с конфиденциальностью?
11. В какой репозиторий публиковать skill — `agent/skills`, `agent/marketing-skills` или новый?
12. Какие дашборды/редаши команды подключать? (URL, workbook_id)
13. Есть ли у пользователя доступ к `dwh-redash-mcp` / `querypad` сейчас?
14. Кто занимается треком B (skeleton MCP), пока ты на треке A? Если ресурса нет — делаем последовательно, гейт сначала.

---

## 10. Критерии готовности проекта

- ✅ Гейт детекторов пройден на 3 документах по **всем пяти трекам раздельно** (recall ≥ 80%, precision ≥ 70% по каждому).
- ✅ Полный корпус (500+ документов) проиндексирован.
- ✅ MCP-сервер задеплоен, наполнен реальной логикой, зарегистрирован в Avito MCP Hub.
- ✅ Skill опубликован, обнаружим через поиск, ставится через `avito ai skills install`.
- ✅ Минимум 3 пользователя из ASD Goods попробовали и дали обратную связь.
- ✅ Документация для пользователей (Confluence или README скилла).

---

## 11. Источники и образцы

- Skill `quality-metrics`: `stash.msk.avito.ru/agent/qa-skills`
- MCP Hub документация: `docs.k.avito.ru/mcp-hub`
- Анонс llm-gateway моделей: канал `~llm-news`, пост от 07.05.2026
- Регламент доступа к LLM: сообщение Александры Шиллер от 27.04.2026
- Кейс Анны Крыжановской (Legal, паттерн «парсинг + анонимайзер + Claude + UI»): AI Demo 29.04.2026, слайды 5–10
- Кейс Артёма Ивангородского (n8n + проактивность): AI Demo 29.04.2026, слайды 28–37
- Кейс Андрея Бровко (`quality-metrics` skill): AI Demo, слайды 39–58
- Бриф Алексея Ли: встреча «[CM] AI - Docs», 07.05.2026

---

## 12. Эскалация

Если Claude Code сталкивается с:
- Неоднозначностью в требованиях → спросить пользователя, не угадывать.
- Гейт-трек не проходит 3 раза подряд → остановиться, эскалация.
- Конфликтом между источниками (например, образец `quality-metrics` противоречит ТЗ) → спросить пользователя; **ТЗ имеет приоритет**.
- Запросом на функциональность вне ТЗ → не реализовывать, добавить в открытые вопросы.
- Аппрув PaaS / mcp-registry застрял → продолжать гейт и индексацию параллельно, не блокироваться.

---

## 13. Поправки v1.4 — по итогам стресс-теста логики (17.05.2026)

Логика системы прошла дуал-агентный челлендж (челленджер ↔ защитник).
Существенные поправки. **При конфликте раздел 13 имеет приоритет над 1–12.**

### 13.1. Переименование: «память + ретрив + LLM», не «слой детекторов»

Формулировка «Veyra = слой детекторов» — неточна. На гейте index-time
детекторы `find_contradictions` (группировка метрик) и `find_logic_signals`
выдали **0**. Обнаружение расхождений выполнил Claude, рассуждая над
поданным контекстом. Точная формулировка:

> **Veyra = память корпуса (индекс + извлечённые факты) + инструменты +
> LLM-рассуждение поверх.** Кастомный код нужен, чтобы Claude не
> перечитывал весь корпус; само обнаружение — работа LLM над контекстом.

### 13.2. НОВАЯ Фаза 1.5 — retrieval-гейт (между Фазой 1 и 2)

Гейт Фазы 1 прошёл в режиме «полные тексты в контексте». На 300+
документах этот режим невозможен — текст не влезает. Продакшн работает
через retrieval, который **не проверялся**.

**Фаза 1 НЕ закрывает Фазу 2.** До индексации полного корпуса обязателен
отдельный гейт: тот же `scripts/gate_ground_truth.json`, но корпус
заведомо не влезает в контекст (>200K символов — раздуть/добавить
документов), прогон строго через retrieval-путь. Критерии — те же
(recall ≥80% / precision ≥70% по трекам). Фаза 2 не стартует, пока
retrieval-гейт не пройден.

### 13.3. Ground truth — нужна человеческая разметка

`scripts/gate_ground_truth.json` размечен Claude по тем же документам;
детектор — тоже Claude. Гейт меряет **самосогласованность модели**, не
правильность. До пилота — независимая разметка расхождений человеком
(аналитик команды). Расхождение с `gate_ground_truth.json` >20%
аннулирует метрики гейта.

### 13.4. Контракт ответа — охват обязателен

Veyra **не отвечает «расхождений нет» без указания охвата проверки**.
Каждый ответ несёт «проверено: N из M документов / такие-то разделы».
«Чисто» без охвата запрещено — защита от тихого ложного негатива
(retrieval посмотрел 5% корпуса, а ответил «всё чисто»).

### 13.5. Смета индексации

До индексации 300+ документов — посчитать смету (число LLM-вызовов ×
цена gateway × объём) и определить режим переиндексации при правках
документов. Без сметы Фаза 2 (Трек D) не стартует.

### 13.6. Вердикт инициативы — отдельный гейт

Строка `verdict` (approve/needs_work/reject) в `check_initiative` — это
суждение, оно галлюциногеннее расхождения и гейтом не проверялось. До
отдельного гейта вердикта (ground truth: 3–5 инициатив с экспертной
оценкой) `check_initiative` отдаёт только расхождения и пробелы, **без**
строки verdict.

### 13.7. Масштабная часть — пока слова, не код

Граф памяти, кластеризация claim'ов по темам, фоновый ночной проход —
спроектированы словами, кода нет. Каждый пункт требует прототипа и
замера ДО того, как на него опираться в плане. Не считать их готовыми.

### 13.8. Достоверность гейта

Один чистый прогон — анекдот, не измерение. Гейт (и Фазы 1, и 1.5)
прогоняется ≥3 раз, recall/precision усредняются. Drop-rate
grounding-фильтра фиксируется отдельной метрикой в `gate_report.md`.
