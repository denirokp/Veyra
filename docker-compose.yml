# AI Lab/Хроника — корпоративная память Avito.
# Поднимает backend (FastAPI + Chroma + SQLite) и frontend (Vite build на nginx).
#
# Запуск:
#   1. cp backend/.env.example .env
#   2. впиши LLM_API_KEY и (опционально) TAVILY_API_KEY / BRAVE_API_KEY
#   3. docker compose up -d
#   4. открыть http://localhost:8080
#
# Данные (Chroma, SQLite, файлы документов, HF-кэш модели эмбеддингов)
# хранятся в именованных volume — переживают перезапуск контейнеров.

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: ai-lab-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      # Перезаписываем пути под структуру volume.
      DOCS_DIR: /app/data/docs
      STYLE_ANCHORS_DIR: /app/data/style_anchors
      CHROMA_PERSIST_DIR: /app/data/chroma
      DATABASE_URL: sqlite+aiosqlite:////app/data/khronika.db
      # В контейнере фронта nginx стоит на 80 → доступ через localhost:8080.
      CORS_ORIGINS: http://localhost:8080,http://localhost:5173
    volumes:
      - ai-lab-data:/app/data
      - ai-lab-hf-cache:/app/.hf_cache
    expose:
      - "8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health', timeout=3)"]
      interval: 30s
      timeout: 5s
      start_period: 120s  # embedding-модель грузится ~30-60 сек на первый старт
      retries: 3

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: ai-lab-frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      backend:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://localhost/ || exit 1"]
      interval: 30s
      timeout: 3s

  # MCP-сервер AI Lab — тонкий фасад над backend, отдаёт корпоративную
  # память как MCP-инструменты. В сети compose backend доступен как
  # http://backend:8000.
  ai-lab-mcp:
    build:
      context: ./ai-lab-mcp
      dockerfile: Dockerfile
    container_name: ai-lab-mcp
    restart: unless-stopped
    environment:
      AILAB_BACKEND_URL: http://backend:8000
      AILAB_MCP_HOST: 0.0.0.0
      AILAB_MCP_PORT: "8765"
    ports:
      - "8765:8765"
    depends_on:
      backend:
        condition: service_started

volumes:
  # БД, Chroma, файлы документов
  ai-lab-data:
  # Кэш HuggingFace моделей — чтобы не качать sentence-transformers каждый раз
  ai-lab-hf-cache:
